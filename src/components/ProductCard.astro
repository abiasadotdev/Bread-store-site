---
const {image, name, description, variant, stock, price} = Astro.props;
---

<div class="bg-gray-50 border-4 border-slate-200 w-40 p-3 rounded-xl shadow-lg flex flex-col justify-between items-center hover:scale-105 hover:duration-200 sm:w-52 sm:h-64 lg:w-64 lg:h-72">
    <img src={image} alt={name} loading="lazy" class="h-36 w-36 rounded-xl sm:w-44 lg:w-64" />
    <div class="flex flex-col justify-start w-full mt-2">
        <h2 class="text-base font-bold text-black">{name}</h2>
        <p class="text-xs font-normal text-black">{description}</p>
        <p>{variant}</p>
    </div>
    <div class="flex justify-between items-center w-full mt-2">
        <p>{stock}</p>
        <p>{price}</p>
    </div>
</div>